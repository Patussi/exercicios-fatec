#include <stdio.h>

int exerciciodezenove() {
    int valor = 0;
    int contador;
    for(valor = 0; valor < 101; valor++){
        if ((valor%2) == 0){
            printf("\n%.d", valor);
        }

    }

}